diff --git a/util_scripts/generate_compliance_report.py b/util_scripts/generate_compliance_report.py
index ae8650c..beddb3d 100644
--- a/util_scripts/generate_compliance_report.py
+++ b/util_scripts/generate_compliance_report.py
@@ -194,10 +194,10 @@ class ComplianceReportGenerator:
         }
         
         # Check for common capability issues
-        if declared_capabilities.get('streaming') and 'capability_results' in self.test_results:
-            streaming_tests = [t for t in self.test_results['capability_results'].get('tests', {}) 
+        if declared_capabilities.get('streaming') and 'capabilities' in self.test_results:
+            streaming_tests = [t for t in self.test_results['capabilities'].get('tests', {}) 
                              if 'stream' in t]
-            if any(self.test_results['capability_results']['tests'][t].get('outcome') == 'FAILED' 
+            if any(self.test_results['capabilities']['tests'][t].get('outcome') == 'FAILED' 
                    for t in streaming_tests):
                 analysis['issues'].append(
                     "Streaming capability declared but tests fail - implementation incomplete"
